export type Exercise = {
  id: string;
  name: string;
  sets: number;
  reps: number;
  weight: number;
  position: number;
};

export type Workout = {
  id: string;
  name: string;
};

export type Equipment = {
  id: string;
  name: string;
};

export type Muscle = {
  id: string;
  name: string;
};
